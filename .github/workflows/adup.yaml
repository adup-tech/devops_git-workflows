name: WIP
on:
  workflow_call:

jobs:
  wip:
    runs-on: ubuntu-latest
    steps:
      - name: echo hello
        run: echo 'hello'
      - uses: wip/action@v1
      - name: echo darkness
        run: echo 'darkness'
